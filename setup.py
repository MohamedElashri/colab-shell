from setuptools import setup, find_packages

setup(
    name="colab-shell",
    version="0.0.1",
    description=" Launch a terminal in Google Colab",
    packages=find_packages(),
    install_requires=[],
    zip_safe=False,
)
